# go的是這樣
task go
  table gotable
  set $trialcorrect 1
  keys b n
  show bitmap frame #1
  delay 500
  part nice_fixpoint
  show bitmap @2 #顯示箭頭（go 訊號）
  readkey @3 500 # 最大反應時間 500 毫秒
  clear -1
  ### feedback
  if STATUS == TIMEOUT
    # 一系列的提示點陣圖
    error
    set $trialcorrect 0
  fi
  if STATUS != TIMEOUT && STATUS != CORRECT
    # 一系列的提示位圖
    error
    set $trialcorrect 0    
  fi
  delay &iti
  save @1 &d RT STATUS &d &d $trialcorrect
